import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Distributed Studio | Build Your Remote Team Anywhere",
  description:
    "Distributed Studio helps growing companies hire, manage, and scale high-performing remote teams. Connect with vetted global talent without the overhead of traditional staffing agencies.",
  keywords: [
    "remote staffing",
    "distributed teams",
    "remote hiring",
    "global talent",
    "staffing agency",
    "remote workers",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
